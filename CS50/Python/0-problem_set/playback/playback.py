#!/usr/bin/env python3

"""
Program prompts the user for input and then outputs that same input, replacing
each space with `...` (i.e., three periods).

"""

prompt = input(">>>").replace(" ", "...")

print(prompt)
